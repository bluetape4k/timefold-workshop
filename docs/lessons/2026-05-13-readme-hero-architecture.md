# README Hero And WIP Refresh

## Context

The Timefold workshop had a short mixed-language root README and no WIP snapshot.

## Decision

Store the generated solver workbench image in `docs/assets/timefold-workbench.png`,
make the root README English, add `README.ko.md`, and create a WIP snapshot from
assigned issues.

## Outcome

The README locale set now presents the solver workshop purpose, modules, and
architecture consistently.

## Verification

- Confirmed the generated asset exists as a PNG under `docs/assets`.
- Verified both README locales reference the image path.
